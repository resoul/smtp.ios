// MARK: - SelectionMode
/// Defines the selection behavior of the Select component
public enum SelectionMode {
    /// Allows selecting only one item at a time
    case single
    /// Allows selecting multiple items
    case multiple
}
